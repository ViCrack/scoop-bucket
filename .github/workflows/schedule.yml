on:
  schedule:
  - cron: '0 * * * *'
name: Excavator
jobs:
  excavate:
    name: Excavator
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@main
    - name: Excavate
      uses: Ash258/Scoop-GithubActions@stable
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        SKIP_UPDATED: '1'
        GIT_USERNAME: "ViCrack"
        GIT_EMAIL: "18179821+ViCrack@users.noreply.github.com"
        GITH_EMAIL: "18179821+ViCrack@users.noreply.github.com"
        BUCKET: "ViCrack/scoop-bucket"
